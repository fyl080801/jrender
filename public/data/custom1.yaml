datasource:

listeners:

fields:
  - type: p
    domProps:
      innerText: custom component
  - type: p
    domProps:
      innerText: $:props.value
  - type: el-form
    children:
      - type: el-input
        formItem:
          label: export
        model: props.value
  - type: slot
