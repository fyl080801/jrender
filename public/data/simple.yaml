# 如果觉得好用，请收藏或则分享本站
listeners:
  - watch: $:GET(model, 'sel')
    actions:
      - handler: $:()=>{console.log('xxxx')}
fields:
  - component: el-form
    options:
      props:
        labelPosition: top
    children:
      - component: el-input
        formItem:
          props:
            label: $:'input:' + GET(model, 'obj.text', '')
        options:
          props:
            value: $:GET(model, 'obj.text')
          attrs:
            placeholder: input value
          on:
            input: $:(e)=>UPDATE(model, 'obj.text', e)
        children:
          - component: span
            options:
              slot: append
              domProps:
                innerText: aaaa
      - component: el-checkbox
        formItem:
          props:
            label: checked
        options:
          props:
            value: $:GET(model, 'checked')
          on:
            input: $:(e)=>UPDATE(model, 'checked', e)
      - component: el-select
        formItem:
          condition: $:GET(model, 'checked')
          props:
            label: select
        options:
          props:
            value: $:GET(model, 'sel')
          on:
            input: $:(e)=>UPDATE(model, 'sel', e)
        items: $:model.arr
      - component: div
        children:
          - component: p
            options:
              domProps:
                innerText: xxx
          - component: p
            domProps:
              innerText: $:model.text
          - component: h1
            children:
              - component: slot
          - component: h2
            children:
              - component: slot
                name: subtitle
      - component: el-button
        options:
          domProps:
            innerText: 添加
          on:
            click: "$:() => model.arr.push({ value: 'vvv' + model.arr.length })"
